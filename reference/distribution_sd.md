# Calculate standard deviation

Calculate standard deviation

## Usage

``` r
distribution_sd(x, remove_na = TRUE, to_numeric = FALSE)
```

## Arguments

- x:

  A vector

- remove_na:

  Logical indicating if NAs should be removed. Defaults to TRUE

- to_numeric:

  Logical indicating if non numeric vectors should be coerced to
  numeric.
