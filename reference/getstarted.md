# Create a project and some starting files

Create a project and some starting files

## Usage

``` r
getstarted(project = "srop2026")
```

## Arguments

- project:

  The name of the folder to be used which is also the name of the
  project. By default, srop2026. You should only run this once for a
  project.
